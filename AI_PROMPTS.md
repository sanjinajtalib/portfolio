# AI Prompts Used During Development

This file lists the prompts given to Claude (Anthropic) while building this
portfolio scaffold, in the order they were used.

1. "Scaffold the app: routes, root layout, navigation, placeholder pages for
   every screen in my spec. Server Components by default, Client Components
   only where I need interactivity."

2. "Set up Tailwind and base design tokens matching my identity kit — soft
   blue primary (#3B82F6), near-black text (#111111), white background."

3. "Build a Nav component with a mobile menu toggle — this needs to be a
   Client Component since it has interactive state."

4. "Create placeholder pages for Home, About, Projects, the CropAI case
   study, and Contact, following the content map: hero claim on Home,
   CropAI leading the Projects grid, and a CTA on every page that ladders
   up to Contact."

5. "Add a health-check page that fetches real data from a public API and
   renders it, as a Server Component with proper error handling."

6. "Run a production build and fix any errors before this is considered
   done."

7. "The build failed — next/font/google can't reach Google's font API in
   this environment. Fix it without changing the visual result."
